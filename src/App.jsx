export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Vite + React</p>
        <h1>The Block buyer app is ready to build.</h1>
        <p className="lede">
          The project now has a working React app scaffolded with Vite, so we
          can start building the auction experience on top of it.
        </p>
      </section>
    </main>
  );
}
